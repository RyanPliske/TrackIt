import UIKit

struct TRTrackerTableViewCellSize {
    
    static var openedHeight: CGFloat {
        return UIScreen.mainScreen().applicationFrame.size.height * 0.35
    }
    
    static var closedHeight: CGFloat {
        return 60.0
    }
    
}